import {
  selectImageSequence,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectImageSequence();

describe('selectImageSequence', () => {

});
